import { OptionsStyleDirective } from './options-style.directive';

describe('OptionsStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionsStyleDirective();
    expect(directive).toBeTruthy();
  });
});
